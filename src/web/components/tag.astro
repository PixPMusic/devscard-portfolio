---
import type { Tag } from '@/types/shared';
import Icon from './icon.astro';
import Typography from './typography.astro';

export interface Props extends Tag {}

const { name, description, icon, iconColor, url } = Astro.props;

const className = /* tw */ 'flex h-6 w-fit items-center gap-x-1.5 rounded';
const customProps = url
  ? ({
      href: url,
      as: 'a',
      target: '_blank',
      rel: 'noopener noreferrer',
      class: `${className} hover:bg-primary-200 dark:hover:bg-primary-800 bg-primary-100 px-2.5 dark:bg-secondary-900`,
    } as const)
  : {
      class: `${className} bg-secondary-100 px-2.5 dark:bg-primary-800 hover:bg-primary-100 dark:hover:bg-secondary-800`,
    };
---

<Typography variant="tag" data-tooltip={description} {...customProps}>
  {icon && <Icon name={icon} color={iconColor} size={16} />}
  {name}
</Typography>
